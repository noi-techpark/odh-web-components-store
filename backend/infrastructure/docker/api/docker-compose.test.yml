version: "3.4"
services:
  api:
    container_name: wcstore_api
    build:
      context: ../../..
      dockerfile: infrastructure/docker/java.dockerfile
      target: test
    env_file: 
      - .env